---
name: debounce
domain: coding
difficulty: medium
description: Implement debounce with cancel and flush
version: "1.0"
outputs:
  - name: solution
    type: string
---

# Debounce

## Step 1 — Implement

@agent copilot
Implement a debounce function in TypeScript with:
- delay parameter
- cancel() method
- flush() method to execute immediately
- Proper TypeScript generics for the wrapped function

@output solution = $AGENT_RESULT
