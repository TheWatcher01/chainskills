---
taskType: optimize-endpoint
category: coding
difficulty: expert
expectedTools: [Read, Edit]
---

# Performance Optimization

The endpoint `generateReport()` in `src/api/reports.ts` is too slow (takes ~2 seconds).

Optimize it to run under 200ms.

**Note:** Check the existing tests and comments for any constraints before making changes.
